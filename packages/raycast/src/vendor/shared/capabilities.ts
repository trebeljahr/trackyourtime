// GENERATED by packages/raycast/scripts/vendor-core.mjs from packages/shared/src/capabilities.ts.
// Do not edit. Change the source file, then run `pnpm vendor:raycast`.
/**
 * Feature gating on the server's declared API level.
 *
 * A store client (the extension, Raycast, the phone apps) is often newer than
 * the self-hosted server it talks to. A client must therefore never offer a
 * feature the server cannot answer: it asks {@link serverSupports} with the
 * server's level, never a release number. The rule for adding an entry is in
 * docs/versioning.md → "Gating a feature on the server".
 */
/**
 * The lowest server API level each capability needs, by capability name.
 *
 * Every capability a client gates on is listed here, including the ones every
 * supported server has — those map to 1. A new tRPC procedure, input field,
 * enum value or sync event kind that a client uses bumps `API_LEVEL` and adds
 * its capability here at the new level, in the same commit;
 * `capabilities.test.ts` in core fails when a level has no capability.
 */
export const REQUIRES_API_LEVEL = {
  /** The version handshake itself: `apiLevel` on `/api/health`, `CLIENT_TOO_OLD`. */
  "version-handshake": 1,
  /** `entries.discard`, the undo of a start. */
  "entries.discard": 1,
  /** `entries.continue`, resuming a previous entry. */
  "entries.continue": 1,
  /** `entries.descriptions`, the description autocomplete. */
  "entries.descriptions": 1,
  /** Tags on entries (`tagIds`) and the `tags.*` procedures. */
  tags: 1,
  /** Several workspaces per person, addressed by `workspaceId`. */
  workspaces: 1,
  /** ZUGFeRD and XRechnung exports (`invoices.export*`). */
  "invoices.einvoice": 1,
  /** `settings.updateNotice`, the opt-in "vX.Y.Z is available" line in Settings. */
  "settings.updateNotice": 2,
  /**
   * `color` on tasks and `entryCount` on `tasks.list`. An older server strips
   * `color` from a create or update rather than refusing it, so a form that
   * offered the picker there would save nothing — hide it instead.
   */
  "tasks.color": 3,
  /**
   * `profile.setAvatar` / `profile.removeAvatar`. An older server has no avatar
   * endpoints, so the profile-picture control is hidden there.
   */
  "profile.avatar": 4,
  /**
   * `invoices.update`, manual `lines` on `invoices.preview` / `create`, and
   * blank invoices (no `from` / `to`). An older server refuses the procedure
   * and strips `lines`, so the web app hides the editor and the blank flow.
   */
  "invoices.lines": 5,
  /**
   * `settings.setBusinessLogo` / `clearBusinessLogo` and `logo` on the
   * profile. An older server answers the procedures with NOT_FOUND, so the
   * upload control is hidden there rather than offered and refused.
   */
  "settings.businessLogo": 6,
} as const satisfies Readonly<Record<string, number>>;

export type Capability = keyof typeof REQUIRES_API_LEVEL;

export const CAPABILITIES = Object.keys(REQUIRES_API_LEVEL) as readonly Capability[];

/**
 * Whether a server at `serverApiLevel` has `capability`.
 *
 * `null` is a server whose level this client has not learned yet — a cold
 * offline launch, or the moment before the first health read. That answers
 * true: hiding every feature until a network round trip lands would flash the
 * screen, and a write the server turns out not to know is held by the offline
 * queue rather than lost. A server that reports no level is 0, and has nothing.
 */
export const serverSupports = (
  capability: Capability,
  serverApiLevel: number | null
): boolean => serverApiLevel === null || serverApiLevel >= REQUIRES_API_LEVEL[capability];

/**
 * The highest level any capability needs. `capabilities.test.ts` compares it
 * with `API_LEVEL`, so a bump without a capability fails.
 */
export const highestCapabilityLevel = (): number =>
  Math.max(...Object.values(REQUIRES_API_LEVEL));

