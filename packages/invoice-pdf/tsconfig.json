{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "composite": true,
    "lib": ["ES2022"],
    "types": ["pdfkit"]
  },
  "comment": "No DOM and no @types/node: this package runs in the browser as well as on the server. `types: [pdfkit]` pulls the pdfkit ambient PDFKit namespace without dragging Node globals in, and the import-guard test proves no node: import slips through.",
  "include": ["src"],
  "exclude": ["src/*.test.ts"]
}
