/**
 * Moved to `@starter/invoice-pdf` so the invoice renderer and the public
 * generator page share one copy. Re-exported here at the path the report
 * renderer (`pdf.ts`) and every test already import from.
 */
export * from "@starter/invoice-pdf/pdf-format";
